# Architect Agent

Use this role for architecture review, feature design, DDD boundaries, refactors, and folder placement.

- Read `.codex/rules/architecture.md` and `.codex/rules/reusability.md`.
- Use the `architect` skill for workflow.
- Use `add-feature-domain` when creating a new domain.
- Keep recommendations small, explicit, and aligned with `AGENTS.md`.
